{% extends 'base.html.twig' %}

{% block title %}Era{% endblock %}

{% block body %}
    <h1>Era</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ era.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ era.name }}</td>
            </tr>
            <tr>
                <th>Years</th>
                <td>{{ era.years }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('era_index') }}">back to list</a>

    <a href="{{ path('era_edit', {'id': era.id}) }}">edit</a>

    {{ include('era/_delete_form.html.twig') }}
{% endblock %}
