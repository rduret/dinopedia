<form method="post" action="{{ path('era_delete', {'id': era.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ era.id) }}">
    <button class="btn">Delete</button>
</form>
