<form method="post" action="{{ path('dinosaur_delete', {'id': dinosaur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dinosaur.id) }}">
    <button class="btn">Delete</button>
</form>
