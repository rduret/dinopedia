{% extends 'base.html.twig' %}

{% block title %}Environment{% endblock %}

{% block body %}
    <h1>Environment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ environment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ environment.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('environment_index') }}">back to list</a>

    <a href="{{ path('environment_edit', {'id': environment.id}) }}">edit</a>

    {{ include('environment/_delete_form.html.twig') }}
{% endblock %}
